import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'online.dsssbpyq.app',
  appName: 'DSSSBPYQ',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
