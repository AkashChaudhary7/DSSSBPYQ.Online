import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'online.dsssbpyq.twa',
  appName: 'DSSSBPYQ',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
